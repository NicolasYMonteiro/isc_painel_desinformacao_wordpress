# Shiny Server interno (nginx faz o proxy público)
run_as shiny;

server {
  listen 3839;

  location / {
    site_dir /srv/shiny-server;
    log_dir /var/log/shiny-server;
    directory_index on;
  }
}
